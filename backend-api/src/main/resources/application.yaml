server:
  port: 8081

spring:
  application:
    name: backend-api
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:postgresql://localhost:5432/condominio_db
    username: postgres
    password: 1204
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      "[hibernate.format_sql]": true
  ai:
    openai:
      api-key: "sk-llave_temporal_de_prueba_para_arrancar"
